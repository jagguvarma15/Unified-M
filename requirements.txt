# Unified-M core dependencies
# Install with: pip install -r requirements.txt

# Core
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.12.0
scikit-learn>=1.4.0

# Validation & config
pydantic>=2.6.0
pyyaml>=6.0.0
python-dotenv>=1.0.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# CLI & logging
typer>=0.9.0
loguru>=0.7.0
python-multipart>=0.0.6

# Parquet support
pyarrow>=15.0.0

# Excel support
openpyxl>=3.1.0

# Holiday calendars (offline, no API key)
holidays>=0.40

# Optional: Redis cache (falls back to in-memory if unavailable)
# redis>=5.0.0

# Optional: DuckDB connector
# duckdb>=1.0.0

# Optional: Database connectors
# psycopg2-binary>=2.9.0  # PostgreSQL
# pymysql>=1.1.0  # MySQL
# pyodbc>=5.0.0  # SQL Server

# Optional: Cloud storage connectors
# boto3>=1.34.0  # AWS S3
# azure-storage-blob>=12.19.0  # Azure Blob Storage

# Optional: Bayesian MMM backends
# pymc>=5.10.0
# pymc-marketing>=0.9.0
# arviz>=0.18.0
# numpyro>=0.15.0  # JAX-based Bayesian
# jax>=0.4.0
# jaxlib>=0.4.0
# google-meridian>=1.0.0  # Google's Bayesian MMM

# Optional: Ad platform connectors
# google-ads>=24.0.0
# facebook-business>=19.0.0

# Optional: OR-Tools for complex optimization
# ortools>=9.9

# Optional: Orchestration
# dagster>=1.6.0
# dagster-webserver>=1.6.0
# prefect>=3.0.0

# Optional: Data quality (enhanced)
# great-expectations>=0.18.0
# pandera>=0.18.0